---
- name: Debug count
  debug:
    msg: "{{ outer_item }}"
    verbosity: 2
  delegate_to: localhost
  tags: ["deploy_vesxi"]

- name: Debug loop index
  debug:
    msg: "{{ ansible_loop.index }}"
    verbosity: 2
  delegate_to: localhost
  tags: ["deploy_vesxi"]

- name: template out deploy.json
  template:
    src: vesxi.json.j2
    dest: vesxi{{ outer_item }}.json
  delegate_to: localhost
  tags: ["deploy_vesxi"]

- name: IP to check
  set_fact:
    current_ip: "{{ vesxi_starting_ip|ipmath(ansible_loop.index0) }}"
  delegate_to: localhost
  tags: ["deploy_vesxi"]

- name: Debug current_ip
  debug:
    msg: "{{ current_ip }}"
    verbosity: 2
  delegate_to: localhost
  tags: ["deploy_vesxi"]

- name: Debug ip_results
  debug:
    msg: "{{ ip_results }}"
    verbosity: 2
  delegate_to: localhost
  tags: ["deploy_vesxi"]

- name: Deploy vesxi{{ outer_item }}
  shell: govc library.deploy -dc={{ vcsa_dc_name }} /{{ content_library_name }}/{{ vesxi_ovf_name }} {{ vesxi_basename }}{{ ansible_loop.index}} -options vesxi{{ outer_item }}.json
  environment:
    GOPATH: "{{ go_path }}"
    PATH: "{{ go_bin }}"
    GOVC_INSECURE: 1
    GOVC_URL: "{{ vcsa_dns_name }}"
    GOVC_USERNAME: "{{ vcsa_username }}@{{ vcsa_sso_domain }}"
    GOVC_PASSWORD: "{{ esx_password }}"
    GOVC_RESOURCE_POOL: "*/Resources"
  when: current_ip not in ip_results
  delegate_to: localhost
  tags: ["deploy_vesxi"]

- name: Deploy vesxi{{ outer_item }}
  shell: govc govc vm.power -on {{ vesxi_basename }}{{ ansible_loop.index}}
  environment:
    GOPATH: "{{ go_path }}"
    PATH: "{{ go_bin }}"
    GOVC_INSECURE: 1
    GOVC_URL: "{{ vcsa_dns_name }}"
    GOVC_USERNAME: "{{ vcsa_username }}@{{ vcsa_sso_domain }}"
    GOVC_PASSWORD: "{{ esx_password }}"
    GOVC_RESOURCE_POOL: "*/Resources"
  delegate_to: localhost
  tags: ["deploy_vesxi"]
#not in ip_results


#- name: Copy using inline content to second file
# # copy:
#  #  content: |
#   #   ip_address={{ item }}
#    #dest: /etc/some_other_config_file.conf